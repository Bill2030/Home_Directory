drinks = ["cofee", "soda","tea"]
dinner = ["pizza","hamburger","hotdog"]
dessert = ["cake", "ice cream"]

food = [drinks, dinner,dessert]
print(food[1][2])
#2 d lists is a multiple group of lists and each element can be
#accessed using index for example if i want to access cake it will be
#print(food[2][0]) because dessert is in theindex of 2 inside food and
#cake is in the index of 0 in the dessert